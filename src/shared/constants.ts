// Shared constants and types used across contexts (background, content, popup).

/** Notion REST API version pinned per spec §3.2 — handles the Data Sources move. */
export const NOTION_API_VERSION = "2025-09-03";

/** MVP recipe document schema version (per spec §4.2.2). */
export const CURRENT_RECIPE_SCHEMA_VERSION = 1;
